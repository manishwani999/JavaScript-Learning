// write a function to print sum of two numbers

function sum(a, b) {
  // parameter

  let sum = a + b;
  console.log("sum =", sum);
}

sum(10, 20); // argument

/* 

what is parameter?
--> parameter are a variable name listed in a function's definition.


what is argument?
--> argument is a actual values passed to the function when it is called.


*/

// return in function

function substraction(a, b) {
  let sub = a - b;
  return sub;
}

console.log(substraction(50, 5));
